import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Create Your Account - Start Free',
  description:
    'Join Atlas to standardize AI prompts across your team. Create your free account and start building a living prompt library today.',
}

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children
}
